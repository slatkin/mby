# Interactive Surface Migration Ledger

This is the stable inventory for issue #603. It tracks interactive ownership,
not the completed render-system migration. The TuiRealm target and complete-
conversion policy are accepted in ADR 0022.

The architecture map and ledger were reviewed on 2026-08-23. The earlier bespoke
component and incremental-migration ADRs were replaced when the project was
reframed as migration of the existing TUI framework to TuiRealm. Rows move from
`legacy` to `component` during the mirror-first implementation stage, then to
`migrated` after teardown.

Ledger verification: all interactive-surface rows were fully migrated on 2026-08-27 after global bridge teardown (5.3d.23-A). The bare `ast-grep scan` is the whole-tree ratchet and must remain clean. The keyboard-endpoint deletion (8.1 + 8.2 of `openspec/changes/remove-legacy-keyboard-endpoint`) added three architectural gates on 2026-08-28 that now enforce the same verification at compile/scan time: `no-crossterm-key-payloads` (rejects `crossterm::event::{KeyEvent,KeyCode,...}` under `src/app/components/**`), `no-raw-fallback-variants` (rejects `Msg::Legacy`, `Msg::GlobalViewKey`, `Msg::Persist`, and the deleted `*Key` request variants under `src/app/components/**`), and `no-second-router-site` (rejects calls to `playback_command_for_key` / `resolve_key` outside `src/app/{router,key_policy,input_resolver,action,shell}.rs`). All three rules scan clean against the migrated codebase and are pinned by per-rule fixtures in `rules/interactive-component-boundary-tests/`.

## Mouse ownership is now in scope (`restore-mouse-support`, #638)

Mouse was accepted-broken for the alpha build under **D16** in
`openspec/changes/migrate-tui-to-tuirealm/design.md` (legacy mouse framework
deleted rather than migrated). `restore-mouse-support` (#638) reverses that: it
rebuilds mouse delivery on TuiRealm subscriptions (ADR 0024) and adds full
per-surface mouse parity, tracked in this ledger like keyboard parity.

Each row's mouse state is recorded in four columns on the ledger table below —
**Mouse owner**, **Gestures**, **Breakpoints verified**, **Verification** —
filled per phase as `restore-mouse-support` lands. Task 5.5 recorded the
overlay/popup/modal rows and the five task-5.2 surfaces, task 6.4 the
music-workspace and narrow-browse rows, and the remaining root/shell rows (root
UI and overlay routing, playback chrome, Library parent, TV workspace, overlay
stack, playback prompts) are recorded below — no row reads `pending`. A surface
that renders at both breakpoints is verified at both; the overlay/popup rows
recorded here were covered by component tests at single fixed geometries, so
their narrow/wide pointer behaviour is the task-5.6 live review, and a row whose
cells record only a fixed test geometry says so for the same reason.

## States

- `legacy`: local state, input, update, rendering, or render-derived interaction
  data still depends on global `App` ownership.
- `component`: the component has landed and paints the surface; the shell still
  mirrors `App` state and/or legacy input still forwards; `App` teardown is
  pending group 5.
- `migrated`: one component owns its approved local boundary; old `App` fields and
  handlers are removed rather than mirrored; static and behavior checks pass.

These are the only committed states. Temporary adapters may exist inside the
complete-conversion change, but a mixed TuiRealm/legacy framework is not a completed
or mergeable endpoint even when individual rows are marked `component`.

## Update Rules

- New independently interactive surfaces receive a row before implementation and
  enter as `legacy`, then change to `component` when their component lands.
- Existing rows change to `component` when their component lands and to
  `migrated` only in the PR that completes and verifies the ownership move.
- Splitting or combining rows requires an update to the architecture map and a
  rationale in issue #603.
- An exception to the legacy no-new-debt rule requires maintainer approval recorded
  in issue #603 and linked from the row's Notes cell. Inline lint ignores are not an
  exception record.
- Rows are never deleted merely because a surface is hidden or temporarily unused.

## Ledger

The **Owner** and **Painter** columns state, per reachable breakpoint (`all:` =
breakpoint-invariant), which mounted component owns the surface's cursor,
scroll, and keyboard handling, and which single painter runs for its rect. This
is D1 of `migrate-narrow-browse-to-components` and the
`interactive-component-framework` spec requirement "Interactive surfaces have
one owner and one painter". A cell reading `none` means the surface is pure
shell derivation with no mounted component / no body painter. The Queue row is
the one open F2 (two painters), deferred to #629.

Canonical media-list destinations use `WideMediaList` for fixed-row Wide rails and Queue, and `InlineMediaBrowser` for Normal/Narrow selected-row replacement. Inline Search is an embedded destination capability: Browser owns and paints it for generic/Movies/homevideos, Emby podcast, and Normal TV; MusicWorkspace owns and paints it for grouped Music; TvWorkspace owns and paints it for Wide TV. Non-hero catalogs retain their existing two-column arrangement. Primary owner/painter mapping: Home — `HomeComponent`; generic Emby, Movies, homevideos, and Emby podcast — `BrowserComponent`; TV Series — `TvWorkspaceComponent` in Wide and `BrowserComponent` in Normal; grouped Music — `MusicWorkspaceComponent`; Audiobookshelf Podcast — `AudiobookshelfPodcastComponent`; Audiobookshelf Books — `AudiobookshelfBookComponent`; Feeds — `FeedsComponent`; Queue — `QueueComponent`.

### #607 acceptance criterion — "component-local interaction state has one owner" (met 2026-08-31)

`delete-browse-level-cursor-scroll` task 4.3
(`openspec/changes/archive/2026-08-31-delete-browse-level-cursor-scroll/tasks.md`,
verified at `2b91957f`) resolved the inventory that carries this criterion. The
recorded result: `BrowseLevel` (`src/app/types_browse.rs`) carries only
`resting: BrowseResting` with private `cursor`/`scroll` reached through
`resting()` / `set_resting_cursor` / `set_resting_scroll` and the sole external
constructor `BrowseResting::new`; `AudiobookshelfBrowseState` and
`AudiobookshelfBookBrowseState` carry only a resting `selected_id`
(`scroll`/`episode_selection`/`chapter_selection`/`selected_bucket` deleted,
component-owned). Every row of
`split-browse-state-interaction-fields/design.md` §1.1 (R1–R13), §1.1b
(R14–R20), and §1.2 resolves to either a threaded `Component::cursor()`
parameter (outcome 1) or a `resting()` copy taken only at a
navigation/load/persist event (outcome 2); no `App` field mirrors a mounted
component's live cursor, scroll, or selection. The surviving `set_resting_*`
writers on live paths are the task-2.3 event-driven persistence seams
(`save_default_library_position`), never read back per frame.

Documented carve-out: `FeedHomeVideoState::video_cursor`
(`src/app/types_feed.rs`) is an `App`-held live cursor for the feed home-video
group view. Authority design D6 scopes it out explicitly ("reads its own
`feed_home_video.video_cursor`, not `BrowseLevel`"); it is not a violation of
this criterion.

| Parent | Interactive surface | Current state | Primary current ownership | Owner (per breakpoint) | Painter (per breakpoint) | Notes | Mouse owner | Gestures | Breakpoints verified | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Shell | Root UI and overlay routing | migrated (2026-08-27) | `UiRootComponent` (`src/app/components/root.rs`) and `shell_root.rs`; TuiRealm owns active focus and native LIFO restoration while the component owns overlay z-order | all: `UiRootComponent` + `shell_root.rs` routing | all: `UiRootComponent` | `App` state and legacy handlers teardown completed in group 5; root UI and overlay stack use `active`/`umount`; `rtk cargo nextest run -p mbv root_ui`; `rtk ast-grep scan`; `rtk make check-code-file-lines`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | Shell (`Model`), not the component: `UiRootComponent::on` forwards **left-click only** as `TerminalObserverEvent::MouseClick` (the sole signal for shell-painted chrome that has no mounted component of its own — the tab bar) and every other mouse kind as `NoOp`; `apply_terminal_observer` (`shell.rs`) resolves it against the painted `layout.tabs_hitmap` and drives `set_library_tab`, and `mouse_eligible_ids`/`sync_mouse_subscriptions` (`shell_library.rs`, D2) own who else may receive a mouse event at all | click on a painted tab glyph switches the library tab (same entry point as keyboard tab-cycling); a click outside `tabs_area` is a no-op; the `«`/`»` scroll-indicator glyphs are deliberately absent from `tabs_hitmap`, so their clicks page nothing; double-click, right-click, wheel and hover-move are never seen by the shell (`NoOp`) | eligibility across a wide/narrow breakpoint change and overlay mount/unmount (unit-tested); the tab click at one fixed tick-harness geometry (80×24, i.e. below the 82-column two-column threshold) — the tab bar itself is breakpoint-invariant chrome | `tab_bar_click_switches_active_tab`, `tab_bar_click_outside_tabs_area_is_noop` (`tests_tick_integration_mouse.rs`); `mouse_eligibility_rung1_blocking_overlay_is_exclusive`, `mouse_eligibility_rung2_topmost_panel_overlay_is_exclusive`, `mouse_eligibility_rung3_is_painted_destination_plus_playback`, `mouse_eligibility_follows_breakpoint_and_overlay_lifecycle`, `sync_mouse_subscriptions_tracks_and_wipes_the_eligible_set` (`shell_library_tests.rs`); `fold_keeps_one_mouse_claim_and_the_observer_signal`, `fold_debug_asserts_on_a_second_claim` (`shell.rs`) |
| Root | Playback chrome and global controls | migrated (2026-08-27) | `PlaybackComponent` (`src/app/components/playback.rs`); shell mirrors reduced playback status and retains Player/transport authority | all: `PlaybackComponent` | all: `PlaybackComponent`; `PanelMode::QueueOnly`: legacy `render_main` player panels (D5 — `player_area` is empty so the component cannot paint there) | Component emits typed `Msg::Playback` intents and owns rendered transport geometry; per-key `playback` precedence remains on legacy `resolve_key` dispatch because it is irreducible; no PlayerProxy or transport state crosses the boundary; right-column player chrome is now `PlaybackComponent`-sole (no legacy underpaint) after `remove-migrated-surface-underpaint` 3.9 removed `render_player_panel` from `paint_legacy_chrome`; the queue-only-mode player panels in `render_main` remain the sole legacy renderer (D5) because `player_area` is empty in queue-only mode so the component cannot paint there; execution ownership proven by `tests.rs::player_chrome_legacy_base_frame_publishes_geometry_but_paints_no_panel`; `rtk cargo nextest run -p mbv playback_chrome` (3 passed); group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `PlaybackComponent`'s own `on`/`mouse` (direct `MouseEvent` matching over the four rects its own `view()` publishes — `play_pause_area`/`stop_area`/`next_area`/`seekbar_area`; no `MouseGestureState`, no `HitRegions` — task 2.7) | left-click = play/pause glyph (`TogglePlayPause`), stop glyph (`Stop`, only while `stop_available`), next glyph (`Next`, only while `next_available`), or a seek-bar click → `SeekTo(fraction)` with the fraction resolved against the component's painted `seekbar_area` (shell `seek_to_fraction` never reads that geometry); an empty published rect resolves to nothing (`PanelMode::QueueOnly`, `show_controls: false`, or the tight-title fallback that drops the stop/next glyphs); no double-click/right-click/wheel/hover recognition | wide and narrow — one painter publishes the same rects at both (`narrow_player` only collapses stop/next); both proofs run at fixed geometries, so narrow/wide pointer behaviour is the phase live review | `seekbar_click_resolves_a_fraction_against_the_painted_seekbar_area`, `transport_button_click_emits_its_typed_intent` (`playback.rs`, fixed painted `player_area` 10,5,40,4, `narrow_player: false`); `tick_delivers_seekbar_click_to_unfocused_playback_as_a_fraction` (`tests_tick_integration.rs`, 60×12 harness — delivery proof from an unfocused state, D2) |
| Root | Queue | migrated (2026-08-27) | `QueueComponent` (`src/app/components/queue.rs`); shell mirrors `PlayerTab` queue snapshots and keeps legacy queue handlers/Player effects | all: `QueueComponent` | all: `QueueComponent` | Queue body has no legacy underpaint; `QueueComponent` is the sole row painter at every breakpoint, with the base frame reserving `queue_area` only; component owns cursor, scroll, scope, queue rendering, and slot-id hit geometry; canonical queue remains Player-owned; `rtk cargo nextest run -p mbv queue`; component, conformance, and shell-routing tests; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | QueueComponent private `MouseGestureState` (Phase 2, task 3.6 of `restore-mouse-support`) | click = scope pill (`QueueScopeClick`) or row select + `QueueRowClick` (resolved `QueueSlotId` via `resolve_point`); double-click = pill or `QueueRowActivate`; right-click = `QueueRowContextMenu` (anchor forwarded, D4); wheel = `QueueScroll`; hover-move unconsumed (D7) | wide and narrow | `queue_right_click_uses_the_rendered_slot_target`, `queue_scope_mouse_pills_reset_component_scroll_from_nonzero` (`queue_component_tests.rs`) |
| Root | Library parent | migrated (2026-08-27) | No component — Library-parent routing is pure derivation over `App.tab` in `src/app/shell_library.rs` (`sync_active_destination` + `library_child_id`), which routes TuiRealm focus to the mounted destination child (`Home`, `Feeds`, `Browser(..)`, ABS/TV/Music workspace) or `UiRoot` when the destination has no mounted surface component | all: none — shell derivation over `App.tab` (`sync_active_destination`) routes focus to the destination child | all: none (focus routing only) | Child components remain unchanged; `ComponentId::Library` was removed and `panel_mode_cycle_x` reassigned to `UiRoot` under `resolve-library-parent-phantom`; `rtk ast-grep scan` is the whole-tree ratchet and must remain clean | none — the row has no mounted component and paints nothing, so there is no geometry to resolve a point against; a mouse event over the library region is delivered to the routed destination child (rung 3 of `mouse_eligible_ids`) and recorded on that child's row | none — no painted surface, hence no meaningful pointer gesture | not applicable (no surface of its own; the wide/narrow switch is recorded on the destination-child rows) | `mouse_eligibility_rung3_is_painted_destination_plus_playback`, `mouse_eligibility_follows_breakpoint_and_overlay_lifecycle` (`shell_library_tests.rs` — the eligible child flips `tv_workspace_id` ↔ `emby_browser_id` at the breakpoint) |
| Library | Home | migrated (2026-08-27) | `HomeComponent` (`src/app/components/home.rs`) with Model-owned Home content and semantic section preference; shell owns fetch/result delivery, restore/persistence, and effect boundaries | all: `HomeComponent` | all: `HomeComponent` | `App.home`/`home_loading`/Home preference fields and `sync_home` are deleted; `push_home_content` is event-driven, `save_prefs` preserves shell-owned `home_section`, and CW/section state has no App owner; content/state/preference ownership and group 5 teardown are complete; `remove-migrated-surface-underpaint` (2026-08-29): legacy base frame reserves only `home_area`; `HomeComponent` is the sole body painter; execution ownership proven by `tests_home_characterization.rs::legacy_base_frame_does_not_paint_home_content_before_the_component`; `rtk cargo nextest run -p mbv` (1152 passed); `rtk ast-grep scan` is the whole-tree ratchet and must remain clean; `rtk cargo fmt --all -- --check`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | HomeComponent private `MouseGestureState` (Phase 2, task 3.5 of `restore-mouse-support`) | click = section pill (`HomePillClick`) or row (`HomeRowClick`); double-click = pill or `HomeRowActivate`; right-click = `HomeRowContextMenu` (anchor forwarded, D4); wheel = `HomeScroll` (step finished by `Model::handle_home_scroll`, preserving the `cw_move_cursor` quirk); hover-move unconsumed (D7) | wide and narrow | `home_right_click_uses_the_rendered_row_target` (`home_component_tests.rs`) |
| Library | Emby generic/Movies/home-video browser | migrated (2026-08-27) | `BrowserComponent` (`src/app/components/browser.rs`); component owns cursor, scroll, destination-embedded Inline Search, and wide/narrow input derivation while shell owns search fetch/index/stale-completion/navigation effects and mirrors typed list context | all: `BrowserComponent` (also owns Emby podcast libraries and the feed/home-video group picker — `is_feed_home_video_group_view`, `BrowserKind::Generic`/`HomeVideos`) | all: `BrowserComponent` including Inline Search (`render_library`'s `EmbyLibrary` arm reserves geometry only) | Cursor/scroll mirrors removed; wide mode derives from the component's `BrowserKey` kind plus painted geometry at the existing breakpoint. Generic/Movies/home-video rows paint through `render_generic_movies_home_video_rows_with_ctx`; music, TV/series, and album-track branches remain legacy for 4.2/4.3/4.4. `remove-migrated-surface-underpaint` (2026-08-29): wide: `BrowserComponent` is the sole painter (execution ownership proven by `tests_non_music.rs::wide_movies_legacy_base_frame_publishes_geometry_but_paints_no_rows`); narrow generic/Movies/home-video body was still legacy-painted at that date (superseded 2026-08-30, below). #613's shared-underpaint concern is resolved — `App::render` is deleted, the draw path is one `Model::draw_frame` entry point with `App::compose_base_frame` composing the base frame beneath the mounted component views, and #613 is fixed for Music (3.4). App teardown completed in group 5; `rtk cargo nextest run -p mbv emby_browser`; component TestBackend and shell-routing tests; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries. `remove-music-workspace-cursor-mirror` (#620): the `last_mirrored_cursor`/`last_mirrored_scroll` echo-suppression pair is deleted; `set_content` no longer adopts `list.cursor`/`list.scroll`/`track_cursor` on any push. The component owns `album_cursor`/`album_scroll` outright; the shell re-anchors them only at a navigation event via `MusicWorkspaceComponent::re_anchor`, fired by the one-shot `Model::music_workspace_reanchor` flag at first mount, group-pill switch, recursive-album activation, and saved-position restore. `last_album_id`'s track-focus reset (content-identity, not echo) is retained. `migrate-narrow-browse-to-components` (2026-08-30): the narrow generic/Movies/home-video body is now `BrowserComponent`-owned and `BrowserComponent`-painted at both breakpoints — 3.3 hoisted the narrow composer into `src/app/components/browser_narrow.rs`, 3.7 relocated the `render_list` poster-prefetch window to the shell beside `paint_home_image`, and 3.8 deleted `render_list` so `render_library`'s `EmbyLibrary` arm reserves geometry only. The 2026-08-29 "narrow ... legacy-painted" status above no longer holds; the narrow body has one owner and one painter (`BrowserComponent`) at every breakpoint. Emby podcast libraries and the feed/home-video group picker mount and paint through this same `BrowserComponent` at both breakpoints (3.5, with `render_feed_home_video_group_view` deleted); wide podcast was previously blank. Feed-picker painter dedup (#623, 2026-09-01): the picker had kept bespoke painters (`render_feed_group_picker_content`, `render_wide_feed_layer`) that re-implemented — with bugs (#634/#637: inline-expansion regression, wide double-paint, dead hit-testing) — what the shared browser already does. Both deleted. Narrow: `render_narrow_browse_with_ctx` fills the shared pill row with feed-group pills (`paint_feed_group_pills_row`) and falls through to the shared row + inline-hero path. Wide: `render_wide_movies` is the sole path — hero-on-left left card + one-column right rail, feed-group pills in the pills slot; no inline expansion in the rail (per `right-panel-arrangements`). Pill click/cycle routing (`handle_mouse_selector_click_emby`, `BrowserCycleGroup`) unchanged | BrowserComponent private `MouseGestureState` (Phase 2, task 3.4 of `restore-mouse-support`) | click = feed-group pill (`BrowserPillClick`) or row (`BrowserRowClick`, resolved via the embedded control's `resolve_point`); double-click = pill or `BrowserRowActivate`; right-click = `BrowserRowContextMenu` (anchor forwarded, D4); wheel = local scroll + `BrowserScroll`; hover-move unconsumed (D7) | wide and narrow | `browser_mouse_uses_the_painted_two_column_cell_for_left_and_right_clicks` (wide); `narrow_canonical_list_click_moves_cursor_and_emits_row_click`, `narrow_canonical_list_double_click_emits_row_activate`, `narrow_canonical_list_right_click_emits_row_context_menu`, `narrow_canonical_list_pill_click_emits_pill_click` (narrow, task 6.2, `085161be`) (`browser_component_tests.rs`) |
| Library | TV workspace | migrated (2026-08-27) | `TvWorkspaceComponent` (`src/app/components/tv_workspace.rs`); Wide owns and paints destination-embedded Inline Search; shell owns search fetch/index/stale-completion/navigation effects and mirrors `TvWideRenderCtx` | wide: `TvWorkspaceComponent` (including Inline Search); narrow: `BrowserComponent` (including Inline Search) | wide: `TvWorkspaceComponent` (including Inline Search); narrow: `BrowserComponent` (including Inline Search) | Two focusable panes with season/episode child targets; `remove-migrated-surface-underpaint` (2026-08-29): wide: `TvWorkspaceComponent` is the sole painter (execution ownership proven by `tv_wide_tests.rs::wide_tv_legacy_base_frame_publishes_geometry_but_paints_no_workspace`); narrow TV was still legacy-painted at that date (superseded 2026-08-30, below). App teardown completed in group 5; `rtk cargo nextest run -p mbv tv_workspace`; component TestBackend and shell sync; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries. `migrate-narrow-browse-to-components` (2026-08-30): narrow TV is now owned and painted by `BrowserComponent` (D4 — `emby_browser_component_id` accepts `BrowserKind::TvShows` when `!is_wide_tv_active()`; `tv_workspace_component_id` keeps the wide half so the two are mutually exclusive at every width), sharing the `browser_narrow.rs` composer (3.4). The 2026-08-29 "narrow TV legacy-painted" status above no longer holds; D5 hand-off persists the resting cursor on the wide↔narrow active-destination pointer flip (2.3). Any TV-specific season/episode chord has no narrow `BrowserComponent` translation and stays dead — a Change D finding, not fixed here. `restore-library-ctrl-shortcuts` (2026-08-31): the TV workspace owns Emby library `Ctrl+P/A/W/S/R` and bare `r`, emitting typed `ShellRequest::EmbyLibrary*` requests for the current series/stack item; the shell routes them through `handle_browser_request` using the selected `emby_library_index()` and existing `App` effect tails, including the legacy series/episode target semantics. The workspace also owns bare `.` (context menu) and `/` (library search) shortcuts | wide: `TvWorkspaceComponent` private `MouseGestureState` + `HitRegions<TvHit>` season pills (`tv_chrome`, adopted from the painter's `layout.tv_wide_season_tabs` in `view()`); narrow: `BrowserComponent`'s own `MouseGestureState` (this row's narrow half is the Browser row above — task 6.2) | wide: click = `TvHitClick` — season pill via `tv_chrome`, series/episode row via the embedded control's `resolve_ordinal_at_y` against the painted list rects, blank Episodes-pane space = `EpisodesPane` — and moves the component's local pane + pane cursor; double-click = `TvHitDoubleClick` (activate); right-click = `TvHitContextMenu` carrying the `(column, row)` anchor (D4, applied by `open_context_menu_at`) and never moves the cursor; wheel over the series list = local `move_rows` + `TvScroll`; hover-move unconsumed (D7). Narrow: the Browser vocabulary (row click/activate/right-click anchor/pill click/wheel) | wide and narrow | `tv_series_clicks_use_the_rendered_series_row_for_left_and_right_clicks` (`tv_workspace_component_tests.rs`, one fixed wide geometry — 100×20 TestBackend, so the wide rail is the only geometry exercised); narrow: `narrow_canonical_list_click_moves_cursor_and_emits_row_click`, `narrow_canonical_list_double_click_emits_row_activate`, `narrow_canonical_list_right_click_emits_row_context_menu`, `narrow_canonical_list_pill_click_emits_pill_click` (`browser_component_tests.rs`, task 6.2); the wide↔narrow mount flip by `mouse_eligibility_follows_breakpoint_and_overlay_lifecycle` (`shell_library_tests.rs`) |
| Library | Grouped Music workspace | migrated (2026-08-27) | `MusicWorkspaceComponent` (`src/app/components/music_workspace.rs`); component owns destination-embedded Inline Search and mirrors the grouped Music render context and cached tracks while shell owns search fetch/index/stale-completion/navigation effects | all: `MusicWorkspaceComponent` including Inline Search | all: `MusicWorkspaceComponent` including Inline Search | Wide album/track workspace; `remove-migrated-surface-underpaint` (2026-08-29): wide: `MusicWorkspaceComponent` is the sole painter after 3.4 restored the `render_list` wide-Music early return dropped in `dce4389d` (execution ownership proven by `tests_music_characterization.rs::wide_music_legacy_base_frame_publishes_geometry_but_paints_no_rows`); narrow Music and album-track were still legacy-painted at that date (superseded 2026-08-30, below). #613 underpaint resolved for Music. App teardown completed in group 5; `rtk cargo nextest run -p mbv music_workspace`; component TestBackend and shell-sync tests; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries. `migrate-narrow-browse-to-components` (2026-08-30): `MusicWorkspaceComponent::view` gained a narrow branch (grouped-album rows + the Model A hero, **not** the wide right-rail track table) (3.6) and `render_music_workspace_component` falls back to `layout.main.left_area` when `wide_music_area` is empty (2.4); with `render_list` deleted (3.8) the component is the sole painter for grouped Music and album-track at both breakpoints. The 2026-08-29 "narrow Music and album-track legacy-painted" status above no longer holds. `restore-library-ctrl-shortcuts` (2026-08-31): at album level, the component owns Emby library `Ctrl+P/A/W/S/R` and bare `r`, emitting typed `ShellRequest::EmbyLibrary*` requests; the shell routes them through `handle_browser_request` using the selected `emby_library_index()` and existing `App` effect tails. Inline track focus retains track-level `Ctrl+P`/`Ctrl+A` precedence. The workspace also owns bare `.` (context menu) and `/` (library search) shortcuts | `MusicWorkspaceComponent` private `MouseGestureState` (task 6.1 of `restore-mouse-support`, resolved B fork, D3) | wide: click/double-click on the right-rail album row resolve via the embedded `WideMediaList::resolve_point` (D6) to `MusicAlbumCursor { kind: Move }`; wide clicks over the left track table resolve via `layout.wide_music_track_at` and stay component-local (no `Msg`, see the "Inline album-track interaction" row below); narrow: click/double-click first test `claim_group_pill` (pill hit under `pill_regions`) and emit relative `MusicGroupSwitch { delta }`, else fall through to `claim_narrow_album` (resolved via the embedded `InlineMediaBrowser::resolve_point`, D6) and emit `MusicAlbumCursor { kind: Move }` (click) or `MusicAlbumActivate` (double-click); narrow right-click resolves the same album row and emits `MusicAlbumContextMenu { anchor }` (D4); hover-move unconsumed (D7); wide wheel over the right rail pages albums via `move_album_rows(±page_rows)` emitting `MusicAlbumCursor { kind: Page }` (PageUp/PageDown equivalent); wide wheel over the track table steps `track_cursor` ±1 component-locally with no `Msg` (j/k equivalent, no-op when no track is focused); narrow wheel over the album list pages albums the same way; wheel over chrome/pills does nothing | wide and narrow | `narrow_music_album_click_selects_and_requests_cursor_move`, `narrow_music_album_double_click_requests_activation`, `narrow_music_group_pill_click_requests_relative_group_switch`, `narrow_music_album_right_click_carries_pointer_anchor`, `wide_music_album_rail_click_still_requests_cursor_move`, `wide_music_rail_wheel_pages_albums`, `wide_music_track_wheel_steps_track`, `narrow_music_list_wheel_pages_albums`, `music_wheel_over_chrome_is_ignored` (`shell_music_workspace_mouse_tests.rs`) |
| Library | Audiobookshelf podcast browser | migrated (2026-08-27) | `AudiobookshelfPodcastComponent` (`src/app/components/audiobookshelf_podcast.rs`); shell mirrors validated ABS content and preserves legacy effects/input forwarding | all: `AudiobookshelfPodcastComponent` | all: `AudiobookshelfPodcastComponent` | Show/episode workspace; component-owned selector/show/episode geometry; `remove-migrated-surface-underpaint` (2026-08-29): the podcast arm of `render_library` reserves only `audiobookshelf_podcast_area`; `AudiobookshelfPodcastComponent` is the sole body painter at both breakpoints (execution ownership proven by `tests_conformance_matrix.rs::abs_podcast_legacy_base_frame_publishes_geometry_but_paints_no_shows` at narrow 60x20 and wide 120x40); the pure-geometry `render_audiobookshelf_library` renderer was deleted (4.1); `App::render` is gone and the draw path is one `Model::draw_frame` entry point with `App::compose_base_frame` composing the base frame beneath the mounted component views (no `self.app.render(f)` underpaint); App teardown completed in group 5; `rtk cargo nextest run -p mbv abs_podcast`; component, App-free render, shell-routing, and existing render tests; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; `split-audiobookshelf-cursor-ownership` (2026-08-29): the duplicate-arithmetic round trip is gone — `AudiobookshelfPodcastShowMove` carries the component-resolved show index applied via `App::select_audiobookshelf_show`, the `move_/jump_audiobookshelf_show_*` delta helpers were deleted, the page-size divergence is closed (only the component's painted stride resolves the movement), and `set_content` no longer adopts `App`'s stale `episode_selection`/`episode_filter`/`scroll` when the selected show is gone; the `AudiobookshelfBrowseState` clone the component holds is still shared with the shell struct, so field-level separation still depends on `split-browse-state-interaction-fields`; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries | AudiobookshelfPodcastComponent private `MouseGestureState` (Phase 3, tasks 4.1/4.5 of `restore-mouse-support`) | wheel = show-cursor page move + `ShowMove` (gesture-owned throttle, task 4.5); click = painted `show_rows` rect or selector bucket → `ShowMove` (rows resolved via the painted `show_rows` hit map at both breakpoints — the wide rail is composed per-frame, so `resolve_point` does not apply, task 4.1); double-click = `OpenOrPlay` episode intent; right-click ignored (no keyboard context-menu equivalent, task 4.6) | wide and narrow | `abs_podcast_row_mouse_selects_the_clicked_show_and_bucket_start`, `abs_podcast_mouse_double_click_emits_open_or_play_and_right_click_ignored` (`audiobookshelf_podcast_component_tests.rs`) |
| Library | Audiobookshelf book browser | migrated (2026-08-27) | `AudiobookshelfBookComponent` (`src/app/components/audiobookshelf_book.rs`); shell mirrors validated ABS book content and preserves legacy effects/input forwarding | all: `AudiobookshelfBookComponent` | all: `AudiobookshelfBookComponent` | Browser/chapter workspace; component-owned replacement/chapter geometry; `remove-migrated-surface-underpaint` (2026-08-29): the Book arm of `render_library` reserves only `audiobookshelf_book_area`; `AudiobookshelfBookComponent` is the sole body painter at both breakpoints (execution ownership proven by `tests_conformance_matrix.rs::abs_book_legacy_base_frame_publishes_geometry_but_paints_no_books` at narrow 60x20 and wide 120x40); the legacy Book renderer was removed in 5.3d.13 and the pure-geometry `render_audiobookshelf_library` renderer was deleted (4.1); `App::render` is gone and the draw path is one `Model::draw_frame` entry point with `App::compose_base_frame` composing the base frame beneath the mounted component views (no `self.app.render(f)` underpaint); App teardown completed in group 5; `rtk cargo nextest run -p mbv abs_book`; component TestBackend and shell-routing tests; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; `split-audiobookshelf-cursor-ownership` (2026-08-29): the duplicate-arithmetic round trip is gone — `AudiobookshelfBookMove` collapsed from 12 deltas to three resolved-value variants (`Book(index)`, `Bucket(pos)`, `ChapterFocus(Option<usize>)`) applied via `App::select_audiobookshelf_book` / `_book_bucket` / `set_audiobookshelf_book_chapter_focus`, the `move_/jump_audiobookshelf_book_*` and `cycle_/focus_*` delta helpers were deleted, the shell projects no page size in (the component's painted `left_area` is the sole stride), and `set_content` no longer adopts `App`'s stale `chapter_selection`/`browser_offset`/`selected_bucket` when the selected book is gone; the `AudiobookshelfBookBrowseState` clone the component holds is still shared with the shell struct, so field-level separation still depends on `split-browse-state-interaction-fields`; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries | AudiobookshelfBookComponent private `MouseGestureState` (Phase 3, tasks 4.1/4.5 of `restore-mouse-support`) | wheel = page-size move + re-request (chapters when chapter focus active, mirroring the PageUp/PageDown focus split); click = painted `book_rows`/`chapter_rows`/selector-bucket rects → resolved `BookMove` requests (painted hit map at both breakpoints — `resolve_point` does not apply, task 4.1); double-click = `Activate` intent; right-click ignored (no keyboard context-menu equivalent, task 4.6) | wide and narrow | `abs_book_mouse_click_resolves_row_and_right_click_is_ignored`, `abs_book_mouse_double_click_emits_activate_intent`, `abs_book_mouse_wheel_pages_the_book_list` (`audiobookshelf_book_component_tests.rs`) |
| Library | Feeds | migrated (2026-08-24) | `FeedsComponent` (`src/app/components/feeds.rs`) with shell-owned refresh/result validation | all: `FeedsComponent` | all: `FeedsComponent` | Grouping, selector, list, inline hero, filtered playback/enqueue selection, unchanged-snapshot cursor preservation, and group-count coverage verified by `rtk cargo nextest run -p mbv feeds`; exhaustive Feeds mouse routing; `remove-migrated-surface-underpaint` (2026-08-29): the Feeds arm of `render_library` reserves only `feeds_area` (no `render_list` delegation); `FeedsComponent` is the sole body painter (execution ownership proven by `tests_conformance_matrix.rs::feeds_legacy_base_frame_publishes_geometry_but_paints_no_entries` at narrow 60x20 and wide 140x30); `App::render` is gone and the draw path is one `Model::draw_frame` entry point with `App::compose_base_frame` composing the base frame beneath the mounted component views (no `self.app.render(f)` underpaint); `rtk ast-grep scan` is the whole-tree ratchet and must remain clean | FeedsComponent private `MouseGestureState` (Phase 3, tasks 4.1/4.5 of `restore-mouse-support`) | wheel = local selection move (no shell request); click = selector pill cycle or row select via the active canonical control's `resolve_point` + `FeedsRowClick` focus request; double-click = `FeedsPlay`; right-click ignored (no keyboard context-menu equivalent, task 4.6) | wide and narrow | `unfocused_component_handles_mouse_input` (wheel), `mouse_owns_feed_selector_and_row_geometry`, `feeds_mouse_click_resolves_row_and_right_click_is_ignored`, `feeds_mouse_double_click_plays_the_resolved_entry` (`feeds_component_tests.rs`) |
| Root | Overlay stack | migrated (2026-08-27) | `UiRootComponent` (`src/app/components/root.rs`) owns overlay z-order; shell mirrors mounted presence while TuiRealm owns focus stack | all: `UiRootComponent` | all: `UiRootComponent` | Open uses `active`, dismiss uses `umount` with native focus restoration; legacy `App` options/flags and handlers remain group 5 teardown satisfied; `rtk cargo nextest run -p mbv root_ui`; `rtk ast-grep scan`; `rtk make check-code-file-lines`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | none for the stack itself — `UiRootComponent` owns overlay z-order only and its `on` never interprets a pointer event; the root's contribution is eligibility: `OVERLAY_IDS` (bottom-to-top) is what `mouse_eligible_ids` rungs 1–2 read to make exactly one overlay mouse-eligible (`shell_library.rs`, D2), and each overlay owns its own gestures on its own row | none — routing/eligibility only; click-to-select and click-to-dismiss live on the individual overlay, popup and modal rows (tasks 5.1/5.2/5.4), never here | not applicable to the routing itself; the exclusivity and suppression proofs run at the fixed 40×16 tick harness, so narrow/wide pointer behaviour is task 5.6 | `mouse_eligibility_rung1_blocking_overlay_is_exclusive`, `mouse_eligibility_rung2_topmost_panel_overlay_is_exclusive`, `mouse_eligibility_follows_breakpoint_and_overlay_lifecycle` (`shell_library_tests.rs`); `tick_blocking_confirm_modal_suppresses_underlying_mouse_activity` (`tests_tick_integration_mouse.rs`) |
| Overlay stack | Global Search sidebar | migrated (2026-08-27) | `SearchSidebarComponent` (`src/app/components/search_sidebar.rs`); shell Model mounts/umounts the sidebar component, component owns sidebar state (query/cursor/scroll/type_filter/loading/results) + 300 ms debounce driven by `UserEvent::Clock`, renders via `application.view()`, emits `Msg::Shell(DismissSearch/SearchActivate)` and `Msg::Service(SearchQuery)` for cross-boundary work | all: `SearchSidebarComponent` | all: `SearchSidebarComponent` | Component-owned debounce; 25 component+render tests + ast-grep clean; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `SearchSidebarComponent` private `MouseGestureState` + `HitRegions<usize>` results and chips (task 5.2) | result click selects (cursor move); result double-click activates (`SearchActivate`, Enter equivalent); chip click sets the type filter (Tab/BackTab equivalent); outside click dismisses (`DismissSearch`, Esc equivalent); query-row click is a no-op; right-click and wheel ignored | one fixed component-test geometry (40×16); also the underlying surface in the 5.3 blocking-modal tick tests (40×16 harness); narrow/wide pointer review is task 5.6 | `search_sidebar_result_click_selects_like_the_arrow_keys`, `search_sidebar_result_double_click_activates_like_enter`, `search_sidebar_chip_click_sets_the_type_filter_like_tab`, `search_sidebar_outside_click_dismisses_like_esc` (`search_sidebar_component_tests.rs`); suppression beneath blocking modals via `tests_tick_integration.rs` |
| Overlay stack | Settings sidebar and setup forms | migrated (2026-08-27) | `SettingsComponent` (`src/app/components/settings.rs`); shell mounts `OverlayId::Settings`, mirrors App display/service state, owns panel hit geometry, and routes setup effects as typed `Msg::Service` | all: `SettingsComponent` | all: `SettingsComponent` | Local settings cursor, service cursor, setup drafts, and mouse geometry; App-free render seam; first-frame setup remains before Remote Service startup; framework teardown completed in group 5; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `SettingsComponent` private `MouseGestureState` + `HitRegions<usize>` rows (task 5.2) | click on a row selects and activates it (Enter/Space equivalent); a click in the Services destination selects and activates the service (`ServiceRequest::ActivateService`); click outside the painted panel dismisses; wheel scrolls by 3 per throttled step; double-click behaves like click | one fixed component-test geometry (40×12); narrow/wide pointer review is task 5.6 | `settings_mouse_click_selects_and_activates_a_row`, `settings_mouse_click_on_a_service_selects_and_activates_it`, `settings_mouse_click_outside_the_painted_panel_dismisses`, `settings_mouse_wheel_scrolls_by_three` (`settings.rs`) |
| Settings | Multiselect popup | migrated (2026-08-27) | `MultiselectComponent` (`src/app/components/multiselect.rs`); shell mounts `PopupId::Multiselect`, mirrors `App.multiselect_popup`, and preserves the legacy close/persistence handler | all: `MultiselectComponent` | all: `MultiselectComponent` | Local cursor/choices; App-free render seam; component and shell-routing tests; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `MultiselectComponent` private `MouseGestureState` + `HitRegions<usize>` rows (task 5.1/5.2 primitives) | row click toggles the checkbox and moves the cursor (Space equivalent); row double-click commits (`MultiselectCommit`, Enter equivalent); outside click commits (the Esc path is also a commit); inside click off the rows is a no-op; right-click and wheel ignored; outside double-click does not commit twice | one fixed component-test geometry (60×16); narrow/wide pointer review is task 5.6 | `multiselect_row_click_toggles_like_space_and_moves_the_cursor`, `multiselect_row_double_click_commits_like_enter`, `multiselect_outside_click_commits_like_esc`, `multiselect_outside_double_click_does_not_commit_twice` (`multiselect_component_tests.rs`); `settings_popup_multiselect_shell_syncs_and_commits_component_choices` (`shell_overlays_tests.rs`) |
| Settings | Library-routes popup | migrated (2026-08-27) | `LibraryRoutesComponent` (`src/app/components/library_routes.rs`); shell mounts `PopupId::LibraryRoutes`, mirrors `App.library_routes_popup`, and preserves legacy route actions | all: `LibraryRoutesComponent` | all: `LibraryRoutesComponent` | Two-stage local cursor; App-free render seam; component and shell-routing tests; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `LibraryRoutesComponent` private `MouseGestureState` + `HitRegions<usize>` rows (task 5.1/5.2 primitives) | row click selects; row double-click enters (`LibraryRoutesEnter`); outside click emits `LibraryRoutesEsc` (the Esc path: closes on PickLibrary, steps back on PickDevice); inside click off the rows is a no-op; right-click and wheel ignored; outside double-click emits nothing after the first Esc | one fixed component-test geometry (60×16); narrow/wide pointer review is task 5.6 | `library_routes_row_click_selects_like_the_arrow_keys`, `library_routes_row_double_click_enters_like_enter_key`, `library_routes_outside_click_follows_the_esc_path`, `library_routes_outside_double_click_emits_nothing_after_the_first_esc` (`library_routes_component_tests.rs`); `settings_popup_library_routes_shell_syncs_and_routes_escape` (`shell_overlays_tests.rs`) |
| Settings | Feed-management popup | migrated (2026-08-27) | `FeedsManageComponent` (`src/app/components/feeds_manage.rs`); shell mounts `PopupId::FeedManage`, mirrors `App.feeds_manage_popup`, and forwards typed keys to legacy handlers | all: `FeedsManageComponent` | all: `FeedsManageComponent` | Local list/form draft; App-free render seam; component and shell-routing tests; `App.feed_tab` reset remains in `feeds_manage_actions.rs`; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `FeedsManageComponent` private `MouseGestureState` + `HitRegions<usize>` rows and `HitRegions<FeedFormField>` fields (task 5.1/5.2 primitives) | list stage: row click selects, outside click dismisses (Esc equivalent); form stage: field click focuses (Tab equivalent; edit-mode URL click is a no-op like the keyboard), outside click cancels; row double-click edits (Enter equivalent); right-click and wheel ignored; outside double-click emits nothing after the first dismiss | one fixed component-test geometry (60×16); narrow/wide pointer review is task 5.6 | `feeds_manage_row_click_selects_like_the_arrow_keys`, `feeds_manage_row_double_click_edits_like_enter`, `feeds_manage_outside_click_dismisses_like_esc`, `feeds_manage_form_field_click_focuses_like_tab`, `feeds_manage_outside_double_click_emits_nothing_after_the_first_dismiss` (`feeds_manage_component_tests.rs`); `settings_popup_feeds_manage_shell_syncs_and_routes_escape` (`shell_overlays_tests.rs`) |
| Overlay stack | Sessions sidebar | migrated (2026-08-27) | `SessionsComponent` with shell-owned session/cast snapshots | all: `SessionsComponent` | all: `SessionsComponent` | 5.3d.23-B5 removed the last legacy no-op arms from `sessions.rs`; keys route through typed `Msg::Shell` (`OpenSessions`/`DismissSessions`/`RefreshSessions`/`SelectSession`/`DetachSessions`); no `App` interaction mirror or legacy `input_session_keys` handler remains (shell `sync`/push of validated session/cast snapshots is the D5 source-of-truth path); zero `Msg::Legacy`/`LegacyTerminalEvent`/crossterm-adapter refs; `rtk cargo nextest run -p mbv sessions`, `rtk cargo check -p mbv`, `rtk ast-grep scan` | `SessionsComponent` private `MouseGestureState` + `HitRegions<usize>` rows (task 5.2) | wheel steps the session cursor; click on a painted session row selects it, clicking the already-selected row connects (`SelectSession`, Enter equivalent); click outside the painted panel dismisses; double-click behaves like click | one fixed component-test geometry (40×12); narrow/wide pointer review is task 5.6 | `sessions_mouse_click_on_selected_row_connects`, `sessions_mouse_click_on_unselected_row_selects_then_second_click_connects`, `sessions_mouse_click_outside_the_painted_panel_dismisses`, `sessions_mouse_wheel_steps_the_cursor` (`sessions.rs`) |
| Overlay stack | Playlists sidebar | migrated (2026-08-27) | `PlaylistsComponent` (`src/app/components/playlists.rs`); shell mirrors playlist snapshots and handles typed playlist effects | all: `PlaylistsComponent` | all: `PlaylistsComponent` | Variable-row geometry and mouse interaction are component-owned for painting/hit testing; duplicated `input_mouse_panels.rs` router removed; App mirror teardown completed in group 5; `rtk cargo nextest run -p mbv playlists`; component geometry and existing render tests; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `PlaylistsComponent` private `MouseGestureState` + `HitRegions<(bool, usize)>` rows (task 5.2) | wheel steps the visible list's cursor (playlists or open items); right-click on an open playlist goes back (`PlaylistsBack`); row click selects (wrapped-row hit geometry); row double-click activates (`PlaylistsActivate`, Enter equivalent); click outside the panel dismisses | one fixed component-test geometry (24×8); narrow/wide pointer review is task 5.6 | `playlists_component_hit_test_uses_wrapped_row_geometry`, `playlists_component_double_click_activates_an_open_playlist_item`, `playlists_component_right_click_on_an_open_playlist_goes_back` (`playlists_component_tests.rs`); the wheel and outside-dismiss gestures have no dedicated unit test yet |
| Playlists | Save-playlist dialog | migrated (2026-08-24) | `SavePlaylistComponent` (`src/app/components/save_playlist.rs`) and shell modal actions | all: `SavePlaylistComponent` | all: `SavePlaylistComponent` | Child of Playlists workflow; App mirror and legacy handler removed; `rtk cargo nextest run -p mbv` | `SavePlaylistComponent` private `MouseGestureState` (task 5.1); no `HitRegions` — one `frame` | outside click dismisses (`SavePlaylistIntent::Dismiss`, Esc equivalent); inside click is a no-op; typing, submit, right-click, and wheel stay keyboard-only | one fixed component-test geometry (60×12); narrow/wide pointer review is task 5.6 | `save_playlist_outside_click_dismisses_like_esc`, `save_playlist_inside_click_is_a_noop` (`save_playlist_component_tests.rs`) |
| Overlay stack | Help sidebar | migrated (2026-08-27) | `HelpComponent` (`src/app/components/help.rs`); shell Model mounts/umounts, intercepts F1, renders via `application.view()` | all: `HelpComponent` | all: `HelpComponent` | Local scroll; destination-derived content; 27 component+render tests + ast-grep clean; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `HelpComponent` private `MouseGestureState` (task 5.2); no `HitRegions` — one `panel_area` | click inside the panel is swallowed; click outside the panel dismisses (double-click included); wheel adjusts the scroll by ±3 per throttled step; hover-move ignored | ad-hoc component panel rect (40×20); narrow/wide pointer review is task 5.6 | `mouse_click_inside_panel_is_swallowed`, `mouse_click_outside_panel_dismisses`, `mouse_double_click_outside_panel_also_dismisses`, `mouse_scroll_down_increments_by_three`, `mouse_scroll_up_decrements_by_three` (`help.rs`) |
| Overlay stack | Context menu | migrated (2026-08-27) | `ContextMenuComponent` (`src/app/components/context_menu.rs`); shell Model mounts/umounts on `App::context_menu` transitions, forwards keys to existing `handle_key_context_menu`, `App::render_context_menu` does placement (writes `layout.context_menu_rect`), component paints via `application.view()` | all: `ContextMenuComponent` | all: `ContextMenuComponent` (`App::render_context_menu` does placement only) | Exclusive overlay with anchor geometry; 4 component+render tests + ast-grep clean; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | `ContextMenuComponent`'s own `handle_mouse` (direct `MouseEvent` matching over `menu_rect`; no `MouseGestureState` — verified as-is by task 5.4) | click on a selectable inner row executes it (`ContextMenuSelect`); click outside the menu dismisses (`ContextMenuDismiss`); hover-move/drag-right follows the highlight; wheel no-op (no mutation of the obscured view, 5.4) | ad-hoc component rects (10×4) plus the tick harness (40×16) for the wheel-exclusivity proof; narrow/wide pointer review is task 5.6 | `mouse_event_selects_selectable_entry`, `mouse_click_outside_the_menu_dismisses_like_esc`, `mouse_wheel_does_not_mutate_the_menu` (`context_menu.rs`); `context_menu_click_select_executes_and_closes_the_menu` (`shell_overlays_tests.rs`); `tick_context_menu_wheel_does_not_mutate_the_obscured_queue` (`tests_tick_integration.rs`) |
| Overlay stack | Selection modal | migrated (2026-08-24) | `SelectionModalComponent` (`src/app/components/selection_modal.rs`) mounted by the shell through `OverlayRequest`; component owns open state, cursor, filters, painted targets, and local input; source-specific effects remain shell-owned | all: `SelectionModalComponent` | all: `SelectionModalComponent` | `App.selection_modal` and `input_selection_modal_keys.rs` removed; open/refresh/dismiss use the existing `pending_overlay` handoff; blocking reads use the shell `blocking_overlay_active` adapter; component and shell boundary tests; `rtk cargo nextest run -p mbv selection_modal` and `rtk ast-grep scan` | `SelectionModalComponent`'s own `handle_mouse` (direct matching over the painted `row_targets`/`selector_targets`; no `MouseGestureState` — verified as-is by task 5.4) | left click on a painted row sets the cursor and emits `SelectionModalActivate`; click on a selector pill sets the filter (`SelectionModalFilterSelected`); click outside the painted panel dismisses (`DismissSelectionModal`); wheel no-op (5.4) | one fixed component-test geometry (60×16); narrow/wide pointer review is task 5.6 | `selection_modal_mouse_row_uses_painted_target`, `selection_modal_mouse_filter_updates_local_state`, `selection_modal_outside_click_dismisses_like_esc`, `selection_modal_wheel_does_not_mutate_the_obscured_view` (`selection_modal_component_tests.rs`); `selection_modal_shell_syncs_and_routes_dismissal` (`shell_overlays_tests.rs`) |
| Overlay stack | Confirm modal | migrated (2026-08-24) | `ConfirmComponent` (`src/app/components/confirm.rs`) and shell modal actions | all: `ConfirmComponent` | all: `ConfirmComponent` | Shared yes/no workflow; component owns modal action state, shell owns effect dispatch; App mirror and legacy handler removed; `rtk cargo nextest run -p mbv` | none locally — `ConfirmComponent::on` is keyboard-only and returns `None` for mouse events; while mounted it is the sole mouse-eligible component (D2 rung 1, `shell_library.rs::mouse_eligible_ids`) | none — no click passes through or beneath: an outside click produces no underlying message and leaves the underlying sidebar's cursor/scroll/filter untouched (5.3 tick proofs) | shell tick harness at one fixed geometry (40×16) only; narrow/wide pointer review is task 5.6 | `tick_blocking_confirm_modal_suppresses_underlying_mouse_activity` (`tests_tick_integration.rs`); `non_keyboard_events_return_none` (`confirm.rs`) |
| Overlay stack | Daemon-lost modal | migrated (2026-08-24) | `DaemonLostComponent` (`src/app/components/daemon_lost.rs`) and shell modal actions | all: `DaemonLostComponent` | all: `DaemonLostComponent` | Process-lifecycle effects remain shell-owned; App mirror and legacy handler removed; `rtk cargo nextest run -p mbv` | none locally — `DaemonLostComponent::on` is keyboard-only and returns `None` for mouse events; while mounted it is the sole mouse-eligible component (D2 rung 1, `shell_library.rs::mouse_eligible_ids`) | none — no click passes through or beneath: an outside click produces no underlying message and leaves the underlying sidebar's cursor/scroll/filter untouched (5.3 tick proofs) | shell tick harness at one fixed geometry (40×16) only; narrow/wide pointer review is task 5.6 | `tick_blocking_daemon_lost_modal_suppresses_underlying_mouse_activity` (`tests_tick_integration.rs`); `non_keyboard_events_return_none` (`daemon_lost.rs`) |
| Overlay stack | Remote-reanchor popup | migrated (2026-08-24) | `RemoteReanchorComponent` (`src/app/components/remote_reanchor.rs`) and shell modal actions | all: `RemoteReanchorComponent` | all: `RemoteReanchorComponent` | Remote reconciliation stays shell-owned; component owns cursor/targets; App mirror and legacy handler removed; `rtk cargo nextest run -p mbv` | none locally — `RemoteReanchorComponent::on` is keyboard-only and returns `None` for mouse events; while mounted it is the sole mouse-eligible component (D2 rung 1, `shell_library.rs::mouse_eligible_ids`) | none — no click passes through or beneath: an outside click produces no underlying message and leaves the underlying sidebar's cursor/scroll/filter untouched (5.3 tick proofs) | shell tick harness at one fixed geometry (40×16) only; narrow/wide pointer review is task 5.6 | `tick_blocking_remote_reanchor_modal_suppresses_underlying_mouse_activity` (`tests_tick_integration.rs`); `non_keyboard_events_return_none` (`remote_reanchor.rs`) |
| Root | Playback prompts (skip-intro/next-up) | migrated (2026-08-27) | `PlaybackPromptComponent` (`src/app/components/playback_prompt.rs`); shell mirrors prompt text/visibility and routes keys to the existing App handlers | all: `PlaybackPromptComponent` | all: `PlaybackPromptComponent` | Status-bar placement and desktop-notification gating preserved; `rtk cargo nextest run -p mbv playback_prompt` (3 passed), component TestBackend and shell-sync tests; `rtk ast-grep scan` is the whole-tree ratchet and must remain clean; group 5 teardown satisfied; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown | none — the surface has no TUI component to receive a pointer event: mpv draws and accepts both the skip-intro and next-up prompts (`docs/architecture/mpv-owned-playback-prompts.md`), and the TUI `PlaybackPromptComponent` named in the ownership cell was deleted by `04191d93`, so this row is a stale ownership record with no live hit geometry | none — no painted surface, hence no meaningful pointer gesture; nothing may pass through to the chrome behind the old status-bar slot | not applicable (no surface; the row is retained per the update rules even though the TUI affordance is gone) | `intro_started_does_not_show_tui_prompt_when_client_does_not_auto_skip`, `intro_started_auto_skips_when_client_prefers_it` (`tests_player_event.rs` — both assert no TUI prompt is left standing); no mouse test exists because no surface exists |
| Library | Inline album-track interaction | migrated (2026-08-27) | `MusicWorkspaceComponent` (`src/app/components/music_workspace.rs`); shell pushes cached tracks and wide/narrow focus enablement; inline track focus is component-owned `track_cursor` (5.3d Album track focus unit) | all: `MusicWorkspaceComponent` | wide: `MusicWorkspaceComponent` (right-rail track table); narrow: none by design (D6 — narrow has no right-rail track table) | Child track cursor/scroll and album-vs-track focus coupling; explicit unfocused narrow mode; App teardown completed in group 5; `rtk cargo nextest run -p mbv album_track`; component local-state test and Music shell sync; `rtk ast-grep scan`; verified zero `Msg::Legacy`/`LegacyTerminalEvent`/LegacyInput/crossterm-adapter refs; global bridge teardown (5.3d.23-A) landed, satisfying group 5 teardown; keep-mounted lifetime: the component stays mounted while its Service library remains in the catalog, the Model `*_id` field is an active-destination pointer, and `reconcile_destination_mounts` retires components for removed libraries | `MusicWorkspaceComponent`'s shared `MouseGestureState` (task 6.1 of `restore-mouse-support`); wide-only — no narrow track table exists (D6, matching the Painter column) | wide click/double-click over the right rail resolve the track row via `layout.wide_music_track_at` against the `wide_music_track_hitmap` populated from `render_wide_media_list_in_box`'s returned `track_content_area` (`src/app/render/components/music_wide.rs`); the hit sets `track_cursor` and `track_list`'s selection directly and returns `None` — the click stays component-local, it does not cross into a `Msg` | wide only (narrow has no right-rail track table by design, D6) | `music_mouse_track_click_stays_component_local` (`shell_music_workspace_mouse_tests.rs`) |

## Verification Record

Each migrated row must link its OpenSpec change, narrow verification command, direct
component tests, shell-routing test, and final static check in Notes. Existing
render characterization remains required but does not by itself prove migration.
